import { MinlengthCloneDirective } from './minlength-clone.directive';

describe('MinlengthCloneDirective', () => {
  it('should create an instance', () => {
    const directive = new MinlengthCloneDirective();
    expect(directive).toBeTruthy();
  });
});
